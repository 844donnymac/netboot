# base-preseed.cfg - Debian 13 (trixie) for iPXE netboot
# Hostname will be: debian-XXXXXX (random 6 alphanum chars)
# SSH keys only (no root password, no user password)

# ==================== CUSTOMIZE HERE ====================
USER="dmcmorris"
FULLNAME="dmcmorris"
SSH_KEYS_URL="https://raw.githubusercontent.com/844donnymac/netboot/main/authorized_keys"
HOSTNAME_PREFIX="debian"
# ========================================================

# Localization
d-i debian-installer/locale string en_US.UTF-8
d-i keyboard-configuration/xkb-keymap select us

# Network (temporary - overridden in late_command if needed)
d-i netcfg/choose_interface select auto
d-i netcfg/get_hostname string unassigned-hostname
d-i netcfg/get_domain string localdomain

# Mirror
d-i mirror/country string manual
d-i mirror/http/hostname string deb.debian.org
d-i mirror/http/directory string /debian
d-i mirror/http/proxy string

# User account (SSH key auth only)
d-i passwd/root-login boolean false
d-i passwd/make-user boolean true
d-i passwd/user-fullname string ${FULLNAME}
d-i passwd/username string ${USER}
d-i passwd/user-password-crypted password !
d-i passwd/user-default-groups string sudo

# Timezone - South Venice, Florida
d-i clock-setup/utc boolean true
d-i time/zone string America/New_York
d-i clock-setup/ntp boolean true

# ==================== Partitioning: Wipe & single large / ====================
d-i partman/early_command string \
  set -e; \
  for dev in $(list-devices disk); do \
    if [ -e "$dev" ]; then \
      dd if=/dev/zero of="$dev" bs=1M count=10 || true; \
      wipefs -a "$dev" || true; \
    fi; \
  done; \
  debconf-set partman-auto/disk "$(list-devices disk | head -n1)"; \
  dmsetup remove_all || true; \
  mdadm --stop --scan || true; \
  pvremove -ff -y /dev/* || true

d-i partman-auto/method string regular
d-i partman-auto/choose_recipe select atomic

d-i partman-partitioning/confirm_write_new_label boolean true
d-i partman/choose_partition select finish
d-i partman/confirm boolean true
d-i partman/confirm_nooverwrite boolean true

d-i partman-lvm/device_remove_lvm boolean true
d-i partman-lvm/confirm boolean true
d-i partman-lvm/confirm_nooverwrite boolean true
d-i partman-md/device_remove_md boolean true
d-i partman-md/confirm boolean true
d-i partman-md/confirm_nooverwrite boolean true

# Packages
d-i tasksel/tasksel/first multiselect standard
d-i pkgsel/include string openssh-server vim less sudo curl wget
d-i base-installer/install-recommends boolean false
d-i pkgsel/upgrade select none
d-i pkgsel/update-policy select none

popularity-contest popularity-contest/participate boolean false

# Bootloader
d-i grub-installer/only_debian boolean true
d-i grub-installer/with_other_os boolean true

# ==================== Late command (fixed + shortened URLs) ====================
d-i preseed/late_command string \
  in-target sh -c ' \
    RANDOM_SUFFIX=$(tr -dc A-Za-z0-9 < /dev/urandom | head -c6); \
    NEW_HOSTNAME=${HOSTNAME_PREFIX}-${RANDOM_SUFFIX}; \
    echo "${NEW_HOSTNAME}" > /etc/hostname; \
    sed -i "s/127.0.1.1.*/127.0.1.1 ${NEW_HOSTNAME}/" /etc/hosts; \
    hostnamectl set-hostname "${NEW_HOSTNAME}" || true; \
  '; \
  in-target mkdir -p /home/${USER}/.ssh; \
  in-target sh -c "wget -qO- '${SSH_KEYS_URL}' > /home/${USER}/.ssh/authorized_keys" || true; \
  in-target chown -R ${USER}:${USER} /home/${USER}/.ssh; \
  in-target chmod 700 /home/${USER}/.ssh; \
  in-target chmod 600 /home/${USER}/.ssh/authorized_keys; \
  in-target sed -i "s/#PasswordAuthentication yes/PasswordAuthentication no/" /etc/ssh/sshd_config || true; \
  in-target sed -i "s/#PermitRootLogin.*/PermitRootLogin no/" /etc/ssh/sshd_config || true; \
  in-target systemctl restart ssh || true